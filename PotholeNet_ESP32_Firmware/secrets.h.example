/*
 * PotholeNet ESP32-CAM — Wi-Fi Credentials
 * 
 * INSTRUCTIONS:
 *   1. Copy this file and rename it to "secrets.h"
 *   2. Change the SSID and password below
 *   3. Keep secrets.h out of version control (it's in .gitignore)
 * 
 * NOTE: Password must be at least 8 characters for WPA2.
 */

#define AP_SSID     "PotholeNet-AP"
#define AP_PASSWORD "potholenet"