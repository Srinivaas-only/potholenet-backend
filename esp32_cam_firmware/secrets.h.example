// Copy this file to secrets.h (in the same folder) and fill in your real
// values. secrets.h is gitignored so your credentials never reach the repo.
#pragma once

#define WIFI_SSID     "YOUR_HOTSPOT_NAME"
#define WIFI_PASSWORD "YOUR_HOTSPOT_PASSWORD"
#define BACKEND_URL   "http://192.168.x.x:8000/detect/dual-mode"
